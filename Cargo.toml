[package]
name = "arrata-lib"
version = "0.3.2"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
bitcode = { version = "0.6.9", default-features = false, features = ["derive"] }
rand = { version = "0.10", default-features = false, features = ["thread_rng"] }
